extend = "../pyproject.toml"

line-length = 119

[lint.isort]
section-order = [
  "future",
  "testing",
  "standard-library",
  "third-party",
  "first-party",
  "local-folder",
]
known-first-party = ["litestar_chameleon"]

[lint.isort.sections]
testing = ["pytest"]
